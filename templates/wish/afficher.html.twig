{% extends 'base.html.twig' %}

{% block title %}Ma page Main!
{% endblock %}

{% block body %}
	<h1 class="text-center pt-5 pb-3">#Demo## wish n°
		{{ wish.id}}
	</h1>

	<div class="card">
		<div class="card-header">
			{{wish.title}}
		</div>
		<div class="card-body">
			<blockquote class="blockquote mb-0">
				<p>{{wish.description}}</p>
				<footer class="blockquote-footer">{{wish.dateCreated|date('Y/m/d')}}
				</footer>
			</blockquote>
		</div>
	</div>

	<a href="{{path('app_wish')}}" class="text-center">la liste des wishes</a>
{% endblock %}
